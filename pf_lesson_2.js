/*
 *
 *   1) What are variables
 *   2) Why use them
 *   -- Example --
 *   - Prerequisites
 *    BASH COMMAND 1: sudo apt update
 *    BASH COMMAND 2: sudo apt install nodejs npm
 *    BASH COMMAND 3: node -v
 *    BASH COMMAND 4: npm -v
 *
 */

let cup = "coffee";
console.log(cup);
